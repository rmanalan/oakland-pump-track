import Nav from "./nav";

export default function Hero() {
  return (
    <header className="hero bg-black bg-center bg-fixed bg-no-repeat   bg-center bg-cover relative">
      <div className="hero-inner">
        <Nav />
      </div>
    </header>
  );
}
